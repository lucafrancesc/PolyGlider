app {
  rabbitmq {
    host = "127.0.0.1"
    port = 5672
    user = "guest"
    password = "guest"
    queue = "orders.placed"
    exchange = "orders.exchange"
    routing-key = "orders.placed"
  }

  db {
    url = "jdbc:postgresql://localhost:5432/polyglider"
    user = "postgres"
    password = "postgres"
    # Set to false to skip Flyway migrations at startup (useful for local dev without Postgres)
    runMigrations = false
  }

  consumer {
    queue-size = 1000
    workers = 4
  }
}
